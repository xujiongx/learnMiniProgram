<!--components/my-bar/my-bar.wxml-->
<!-- 自定义组件练习 -->
<view class='tab-control'>
<block wx:for="{{title}}" wx:key="{{index}}">
<view class='tab-item {{currentIndex==index?"active":""}}'
      bind:tap="onItemClick"
      data-index="{{index}}">
      
<text>{{item}}</text>

</view>
</block>
</view>